apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-needs-dashboard.fullname" . }}
  labels:
    {{- include "basic-needs-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-needs-dashboard.selectorLabels" . | nindent 4 }}
